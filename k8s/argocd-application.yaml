apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: ml-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/dhruvilsatani/MLOPS.git
    targetRevision: main
    path: k8s/ml-app-chart

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true  
